import React from "react";
import { Link } from "react-router-dom";

function Header(){
    return(
        <nav>
            <a href="#home"> Home </a>
            <a href="#about"> about </a>
        </nav>
    )
}

export default Header;